version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (security patches + version bumps).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  # Rust dependencies.
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
